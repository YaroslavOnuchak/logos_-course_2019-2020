for (let a = -165; a < 0; a++) {
    // console.log(a)
    let ressult = 2 * a + 200;
    if (ressult < -9 || ressult > 9) {

        if (ressult > -100 && ressult < 100) {
            console.log(ressult)
        }
    }
}